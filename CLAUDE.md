# Video Generator

## Database

- **PostgreSQL** via Prisma ORM
- Les noms de tables doivent être en **snake_case** (underscore). Utiliser `@@map("nom_table")` dans le schema Prisma pour mapper les models PascalCase vers des tables snake_case.
